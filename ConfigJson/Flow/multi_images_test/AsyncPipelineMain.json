{
  "id": "async-pipeline-main",
  "name": "Async Pipeline Main",
  "description": "Init shared counter + launch 4 preprocess workers. Each worker claims the next image index atomically, reads at 80ms pace, preprocesses in parallel, then serializes GPU inference via a named lock.",
  "version": "1.0.0",
  "startNodeId": "node-init-index",
  "nodes": [
    {
      "id": "node-init-index",
      "type": "SetSharedVariable",
      "name": "Init NextIndex=0",
      "description": "Shared atomic counter; workers increment to claim the next image",
      "x": 50,
      "y": 200,
      "properties": { "Key": "NextIndex", "Value": "0" }
    },
    {
      "id": "node-init-total",
      "type": "SetSharedVariable",
      "name": "Init TotalCount",
      "description": "Total number of images to process (workers exit when NextIndex >= TotalCount)",
      "x": 280,
      "y": 200,
      "properties": { "Key": "TotalCount", "Value": "50" }
    },
    {
      "id": "node-parallel",
      "type": "Parallel",
      "name": "Launch 4 Workers",
      "description": "Fan out 4 identical worker sub-flows; all share the same SharedFlowContext",
      "x": 510,
      "y": 200,
      "properties": {
        "FlowPaths": "ConfigJson/Flow/AsyncPipelineWorker.json,ConfigJson/Flow/AsyncPipelineWorker.json,ConfigJson/Flow/AsyncPipelineWorker.json,ConfigJson/Flow/AsyncPipelineWorker.json",
        "WaitMode": "All",
        "TimeoutMs": "120000"
      }
    }
  ],
  "connections": [
    { "id": "c1", "sourceNodeId": "node-init-index", "targetNodeId": "node-init-total" },
    { "id": "c2", "sourceNodeId": "node-init-total", "targetNodeId": "node-parallel" }
  ],
  "globalVariables": []
}

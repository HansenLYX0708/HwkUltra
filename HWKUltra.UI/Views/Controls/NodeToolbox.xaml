<UserControl x:Class="HWKUltra.UI.Views.Controls.NodeToolbox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:HWKUltra.UI.Helpers"
             xmlns:models="clr-namespace:HWKUltra.UI.Models"
             Background="{DynamicResource ApplicationBackgroundBrush}">

    <UserControl.Resources>
        <converters:StringToBrushConverter x:Key="StringToBrush" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Node Toolbox" FontWeight="SemiBold" FontSize="14"
                   Margin="12,8,12,4" />

        <!-- Search -->
        <ui:TextBox Grid.Row="1" Margin="8,4"
                    PlaceholderText="Search nodes..."
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    Icon="{ui:SymbolIcon Search24}" />

        <!-- Category Tree -->
        <TreeView Grid.Row="2" Margin="4,4"
                  ItemsSource="{Binding FilteredCategories}"
                  BorderThickness="0"
                  Background="Transparent"
                  ScrollViewer.CanContentScroll="False"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type models:NodeCatalogCategory}"
                                          ItemsSource="{Binding Entries}">
                    <!-- Category Header -->
                    <StackPanel Orientation="Horizontal" Margin="0,4">
                        <Ellipse Width="10" Height="10" Margin="0,0,6,0"
                                 Fill="{Binding Color, Converter={StaticResource StringToBrush}}" />
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                        <TextBlock Text="{Binding Entries.Count, StringFormat=' ({0})'}" Opacity="0.5" FontSize="12"
                                   VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                    </StackPanel>

                    <!-- Node Entry Template -->
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:NodeCatalogEntry}">
                            <Border Padding="6,4" Margin="0,1" CornerRadius="4"
                                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                                    BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    Cursor="Hand"
                                    MouseMove="NodeEntry_MouseMove"
                                    Tag="{Binding}">
                                <StackPanel Orientation="Horizontal">
                                    <Rectangle Width="4" Height="16" RadiusX="2" RadiusY="2" Margin="0,0,6,0"
                                               Fill="{Binding Color, Converter={StaticResource StringToBrush}}" />
                                    <TextBlock Text="{Binding DisplayName}" FontSize="12"
                                               VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="True" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </Grid>
</UserControl>

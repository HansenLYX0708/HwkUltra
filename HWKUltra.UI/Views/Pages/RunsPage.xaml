<Page
  x:Class="HWKUltra.UI.Views.Pages.RunsPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  xmlns:local="clr-namespace:HWKUltra.UI.Views.Pages"
  Title="RunsPage"
  d:DesignHeight="600"
  d:DesignWidth="1000"
  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
  ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  mc:Ignorable="d">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Test Runs (Live)" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,8" />

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
      <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="6" Padding="12,6" Margin="0,0,8,0">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="Active: " Opacity="0.8" />
          <TextBlock Text="{Binding ViewModel.ActiveCount, Mode=OneWay}" FontWeight="SemiBold" Margin="4,0,0,0" />
        </StackPanel>
      </Border>
      <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="6" Padding="12,6">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="Completed: " Opacity="0.8" />
          <TextBlock Text="{Binding ViewModel.CompletedCount, Mode=OneWay}" FontWeight="SemiBold" Margin="4,0,0,0" />
        </StackPanel>
      </Border>
    </StackPanel>

    <DataGrid Grid.Row="2"
              ItemsSource="{Binding ViewModel.Rows, Mode=OneWay}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserAddRows="False"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              Background="Transparent"
              RowBackground="Transparent"
              AlternatingRowBackground="{DynamicResource ControlFillColorDefaultBrush}"
              BorderThickness="0">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Run Key" Binding="{Binding RunKey}" Width="160" />
        <DataGridTextColumn Header="Tray" Binding="{Binding TrayName}" Width="120" />
        <DataGridTextColumn Header="Serial" Binding="{Binding Serial}" Width="140" />
        <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="110" />
        <DataGridTextColumn Header="Defects" Binding="{Binding DefectCount}" Width="80" />
        <DataGridTextColumn Header="Grid" Width="80">
          <DataGridTextColumn.Binding>
            <MultiBinding StringFormat="{}{0}x{1}">
              <Binding Path="Rows" />
              <Binding Path="Cols" />
            </MultiBinding>
          </DataGridTextColumn.Binding>
        </DataGridTextColumn>
        <DataGridTextColumn Header="Started" Binding="{Binding StartTime, StringFormat=HH:mm:ss}" Width="90" />
        <DataGridTextColumn Header="Ended" Binding="{Binding EndTime, StringFormat=HH:mm:ss}" Width="90" />
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</Page>
